export default function AppLogoIcon({ className }: { className?: string }) {
    return (
        <span className={`font-black leading-none text-brand ${className ?? ''}`}>
            b.
        </span>
    );
}
