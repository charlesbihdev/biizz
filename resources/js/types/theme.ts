/**
 * Theme types and runtime values — all derived from Themes/registry.ts.
 * To add a new theme, edit Themes/registry.ts only. Nothing here changes.
 */
export type { ThemeId, ThemeRegistry } from '@/Themes/registry';
export { SCHEMA_MAP, THEME_MAP } from '@/Themes/registry';
